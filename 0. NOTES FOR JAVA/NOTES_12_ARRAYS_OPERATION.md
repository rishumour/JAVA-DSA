# first import java.util.Arrays


1-> Arrays.toString(arr) // used to convert the whole array into string

2-> Arrays.sort(arr) // used to sort the whole array

3-> Arrays.sort(arr, to, from) // used to sort the array from a certain point to a certain point
